import ProjectNav from "@/components/ProjectNav";

interface CalenderViewProps {
  params: {
    slug: string;
  };
}

export default function CalenderView({ params: { slug } }: CalenderViewProps) {
  return <main>CalenderView</main>;
}
